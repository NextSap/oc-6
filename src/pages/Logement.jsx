import React from 'react';
import {useParams} from "react-router-dom";

const Logement = () => {
    const { id } = useParams();
    return (
        <div>

        </div>
    );
};

export default Logement;